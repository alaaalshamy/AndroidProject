<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eg.iti.losh.splash">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Spalsh">


        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".AppIntroClass"
            android:label="Activity of app intro" />
        />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".addTrip">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivityasmeen">

        </activity>
        <activity android:name=".TripDeatails"></activity>

        <activity android:name=".AlarmReceiverActivity"></activity>
        <service android:name=".ScheduleService" />
        <service android:name=".NotifyService" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBC9wL-AA5EY8RadjH4A8_2j1snI8AnoNQ"/>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBC9wL-AA5EY8RadjH4A8_2j1snI8AnoNQ"/>

    </application>

</manifest>